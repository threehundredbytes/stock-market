app:
  auth-server:
    protocol: http
    host: localhost
    port: 8180
    realm: stock-market
    issuer-uri: ${app.auth-server.protocol}://${app.auth-server.host}:${app.auth-server.port}/realms/${app.auth-server.realm}
    jwk-set-uri: ${app.auth-server.issuer.uri}/protocol/openid-connect/certs
    client:
      frontend:
        id: stock-market-frontend
      backend:
        id: stock-market-backend
        secret: '{cipher}b9ed1c84fcdb6f965b91d520c422c9cae2558fe7064c5cfc2e7fb00683d0ce05bbd1666f03b38b352716755e6f21e48443153aa97d5a94319ea0e30d0407675d'